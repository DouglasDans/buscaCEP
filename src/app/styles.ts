const styles = {
  mainContainer:{
    // bgColor: 'background.body',
    width: '100vw',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  },
  cepContainer: {
    width:'800px',
    height: 'auto',
    borderRadius: 'var(--joy-radius-sm)'
  }
}

export default styles;